<p>タスクを表示します</p>

<ul>
	<% @tasks.each do |task| %>
		<li><%= link_to task.id, task_path(task) %>:ステータス：<%= task.status%>　タスク：<%= task.content %></li>
	<% end %>
</ul>
<%= link_to "タスクの新規作成", new_task_path %>
